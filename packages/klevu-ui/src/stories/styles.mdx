import { Meta } from "@storybook/addon-docs"

<Meta title="Guides/Styling components" />

# Styling components

Klevu Components are designed to be easy and flexible, allowing you to customize your project in just a few clicks in this Storybook.
With our extensive selection of CSS variables - accessible from the 'CSS custom properties' tab - tweaking any component is effortless so you can quickly
make modifications and get precisely the look that best suits your needs.

Here is the list of available variables. Replace the values with the ones you tweaked in here Storybook and place this top of your main CSS file.

```css
:root {
  font-family: Arial, Helvetica, sans-serif;

  /** colors */
  --klevu-color-primary: #2b4af7;
  --klevu-color-primary-darker: #0d2ee8;

  --klevu-color-neutral-01: #ffffff;
  --klevu-color-neutral-02: #f6f6f6;
  --klevu-color-neutral-03: #ededed;
  --klevu-color-neutral-04: #e3e3e3;
  --klevu-color-neutral-05: #d6d6d6;
  --klevu-color-neutral-06: #919191;
  --klevu-color-neutral-07: #757575;
  --klevu-color-neutral-08: #191919;

  --klevu-color-accent-01: #8eedd3;
  --klevu-color-accent-02: #ffcb9f;
  --klevu-color-accent-03: #ffb8d6;
  --klevu-color-accent-04: #c3d7ff;

  /** spacing */
  --klevu-spacing-01: 1px;
  --klevu-spacing-02: 4px;
  --klevu-spacing-03: 8px;
  --klevu-spacing-04: 12px;
  --klevu-spacing-05: 16px;
  --klevu-spacing-06: 24px;
  --klevu-spacing-07: 32px;
  --klevu-spacing-08: 40px;
  --klevu-spacing-09: 48px;
  --klevu-spacing-10: 64px;

  /** sizes */
  --klevu-size-s: 16px;
  --klevu-size-m: 20px;
  --klevu-size-l: 24px;
  --klevu-size-xl: 36px;

  /** opacity */
  --klevu-opacity-disabled: 0.35;
  --klevu-opacity-subtle: 0.25;

  /** headings */
  --klevu-h1-size: 24px;
  --klevu-h1-line-height: calc(28em / 24);
  --klevu-h1-weight: 700;

  --klevu-h2-size: 20px;
  --klevu-h2-line-height: calc(24em / 20);
  --klevu-h2-weight: 700;

  --klevu-h3-size: 16px;
  --klevu-h3-line-height: calc(20em / 16);
  --klevu-h3-weight: 700;

  --klevu-h4-size: 14px;
  --klevu-h4-line-height: calc(18em / 14);
  --klevu-h4-weight: 700;

  /** body text */
  --klevu-body-l-size: 20px;
  --klevu-body-l-line-height: calc(28em / 20);

  --klevu-body-m-size: 16px;
  --klevu-body-m-line-height: calc(24em / 16);

  --klevu-body-s-size: 14px;
  --klevu-body-s-line-height: calc(20em / 14);

  --klevu-body-xs-size: 12px;
  --klevu-body-xs-line-height: calc(16em / 12);

  /** border radiuses */
  --klevu-border-radius-xs: 2px;
  --klevu-border-radius-s: 4px;
  --klevu-border-radius-m: 8px;
  --klevu-border-radius-l: 12px;
  --klevu-border-radius-xl: 16px;
  --klevu-border-radius-xxl: 24px;

  /** Elevations, set as box shadow */
  --klevu-elevation-1: 0 0.5px 2px rgba(0, 0, 0, 0.06), 0 3px 6px rgba(0, 0, 0, 0.08);

  /* animation css variables */
  --klevu-animation-duration-slow: 0.3s;
  --klevu-animation-duration-normal: 0.2s;
  --klevu-animation-duration-fast: 0.1s;
  --klevu-animation-timing-function: ease-in-out;

  /** component specific variables */
  --klevu-product-image-aspect-ratio: 1.3 / 1;
  --klevu-product-width: 300px;
  --klevu-product-small-width: 200px;
  --klevu-product-image-fill: cover; /* cover, contain */
}
```

# Slots

The web components library offers an variety of options for customization, with
[slot elements](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/) allowing developers to
alter and personalize the contents of their components - whether partially or fully.

These slots can be found in addons panel's control sections.

Product example:

```html
<klevu-product>
  <div slot="image">This element replaces product image</div>
  <div slot="bottom">This is appended to end of the product</div>
</klevu-product>
```

# ::part

To modify the style and looks of components without replacing HTML content of components there are parts
defined in the components. You can modify the these parts with [::part pseudo-element](https://developer.mozilla.org/en-US/docs/Web/CSS/::part)
CSS selector.

These parts can be found in addons panel's control sections.

Product example:

```css
/* this allows to modify the container div of product */
klevu-product::part(product-container) {
  border: 3px dashed orange;
}
```

## Naming convetion for parts

Parts can be exported from child components to parent components. To avoid name collisions
all parts prefixed with component name, but dropping the `klevu-` from the name. For example
`klevu-product` component has part `product-container`.

Exported parts of components are tracked in the `utils/parts.ts` file.
