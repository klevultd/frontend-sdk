import { Canvas, Meta, Description } from "@storybook/addon-docs"
import * as Stories from "./klevu-cms-list.stories"

<Meta title="Components/Cms List" component="klevu-cms-list" argTypes={meta.argTypes} parameters={meta.parameters} />

# Klevu-cms-list

<Description>{meta.description}</Description>

<Canvas>
  <Story
    name="Cms List"
    args={{
      link: true,
      pages: [
        {
          name: "Google",
          url: "https://www.google.com",
        },
        {
          name: "Facebook",
          url: "https://www.facebook.com",
        },
      ],
    }}
  >
    {(args) =>
      html`<klevu-cms-list
        link=${ifDefined(args.link)}
        .pages=${args.pages}
        caption=${ifDefined(args.caption)}
      ></klevu-cms-list>`
    }
  </Story>
</Canvas>

<ArgsTable story="Cms List" />
