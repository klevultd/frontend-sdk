import { ArgsTable, Canvas, Meta, Story, Description } from "@storybook/addon-docs"
import { html } from "lit-html"
import { ifDefined } from "lit-html/directives/if-defined.js"
import { MDXAutoFillMeta } from "../../storybookUtils"
const meta = MDXAutoFillMeta("klevu-button")

<Meta title="Atoms/Button" component="klevu-button" argTypes={meta.argTypes} parameters={meta.parameters} />

# Klevu-button

<Description>{meta.description}</Description>

<Canvas>
  <Story
    name="Button"
    argTypes={{
      text: {
        description: "Text to be displayed on the button",
        control: {
          type: "text",
        },
      },
    }}
    args={{
      text: "Hello Button",
    }}
  >
    {(args) =>
      html` <klevu-button
        disabled=${ifDefined(args.disabled)}
        full-width=${ifDefined(args.fullWidth)}
        is-secondary=${ifDefined(args.isSecondary)}
        >${args.text}</klevu-button
      >`
    }
  </Story>
</Canvas>

<ArgsTable story="Button" />

# Icon buttons

<Canvas>
  <Story
    name="Icon button 1"
    args={{
      icon: "search",
    }}
  >
    {(args) =>
      html` <klevu-button
        disabled=${ifDefined(args.disabled)}
        full-width=${ifDefined(args.fullWidth)}
        is-secondary=${ifDefined(args.isSecondary)}
        icon=${ifDefined(args.icon)}
      ></klevu-button>`
    }
  </Story>
  <Story
    name="Icon button 2"
    args={{
      icon: "chevron_left",
    }}
  >
    {(args) =>
      html` <klevu-button
        disabled=${ifDefined(args.disabled)}
        full-width=${ifDefined(args.fullWidth)}
        is-secondary=${ifDefined(args.isSecondary)}
        icon=${ifDefined(args.icon)}
      ></klevu-button>`
    }
  </Story>
  <Story
    name="Icon button 3"
    args={{
      icon: "chevron_right",
    }}
  >
    {(args) =>
      html` <klevu-button
        disabled=${ifDefined(args.disabled)}
        full-width=${ifDefined(args.fullWidth)}
        is-secondary=${ifDefined(args.isSecondary)}
        icon=${ifDefined(args.icon)}
      ></klevu-button>`
    }
  </Story>
  <Story
    name="Icon button 4"
    args={{
      icon: "settings",
    }}
  >
    {(args) =>
      html` <klevu-button
        disabled=${ifDefined(args.disabled)}
        full-width=${ifDefined(args.fullWidth)}
        is-secondary=${ifDefined(args.isSecondary)}
        icon=${ifDefined(args.icon)}
      ></klevu-button>`
    }
  </Story>
</Canvas>
