import { ArgsTable, Canvas, Meta, Story, Description, Source } from "@storybook/addon-docs"
import { html } from "lit-html"
import { ifDefined } from "lit-html/directives/if-defined.js"
import { MDXAutoFillMeta } from "../../storybookUtils"
import * as Stories from "./klevu-sort.stories"

<Meta title="Components/Sort" component="klevu-sort" argTypes={meta.argTypes} parameters={meta.parameters} />

# Klevu-sort

<Description>{meta.description}</Description>

<Canvas>
  <Story
    name="Sort"
    args={{
      variant: "default",
    }}
  >
    {(args) => html`<klevu-sort variant=${args.variant}></klevu-sort>`}
  </Story>
</Canvas>

<ArgsTable story="Sort" />
