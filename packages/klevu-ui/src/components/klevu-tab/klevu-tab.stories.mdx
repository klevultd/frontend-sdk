import { ArgsTable, Canvas, Meta, Story, Description } from "@storybook/addon-docs"
import { html } from "lit-html"
import { ifDefined } from "lit-html/directives/if-defined.js"
import { MDXAutoFillMeta } from "../../storybookUtils"
const meta = MDXAutoFillMeta("klevu-tab")

<Meta title="Atoms/Tab" component="klevu-tab" argTypes={meta.argTypes} parameters={meta.parameters} />

# Klevu-tab

<Description>{meta.description}</Description>

<Canvas>
  <Story
    name="Tab"
    args={{
      caption: "Tab",
    }}
  >
    {(args) =>
      html`<klevu-tab caption=${ifDefined(args.caption)}></klevu-tab
        ><klevu-tab caption=${ifDefined(args.caption)} active></klevu-tab
        ><klevu-tab caption=${ifDefined(args.caption)}></klevu-tab
        ><klevu-tab caption=${ifDefined(args.caption)}></klevu-tab>`
    }
  </Story>
</Canvas>

<ArgsTable story="Tab" />
