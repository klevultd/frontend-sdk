import { Controls, Canvas, Meta, Markdown } from "@storybook/addon-docs"
import { KlevuAutoDocs } from "../../../.storybook/blocks/KlevuAutoDocs"
import * as Stories from "./klevu-slider.stories"

<Meta title="Atoms/Slider/Examples/Slider with tooltips" />

## Slider with tooltips

By setting the `formatTooltip` property, you can format the tooltip values.

<Canvas of={Stories.SliderWithTooltips} />

Here is the code for the above example in React (@klevu/ui-react):

```jsx
import React from "react"
import { KlevuSlider } from "@klevu/ui-react"

const MyComp = () => {
  return (
    <KlevuSlider formatTooltip={(value) => `$${value.toFixed(0)}`} end={90} max={100} min={0} start={10} showTooltips />
  )
}
```

<KlevuAutoDocs of={Stories} />
