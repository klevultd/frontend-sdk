import { ArgsTable, Canvas, Meta, Story, Description, Source } from "@storybook/addon-docs"
import { html } from "lit-html"
import { ifDefined } from "lit-html/directives/if-defined.js"
import { MDXAutoFillMeta, fullMockRequest, mockProducts } from "../../storybookUtils"
import { productWithCustomizedProduct, singleProduct } from "./story/product"
const meta = MDXAutoFillMeta("klevu-product")
const productItem = mockProducts[0]
const productItem2 = fullMockRequest.queryResults?.[0].records[1]
const productItem3 = fullMockRequest.queryResults?.[0].records[2]

<Meta title="Components/Product" component="klevu-product" argTypes={meta.argTypes} parameters={meta.parameters} />

# Klevu-product

<Description>{meta.description}</Description>

## Default product

Default product has bigger image and more information than rest of the variants.

Here are four products.

1. Without any modifications
2. Style modifications with CSS properties through style attribute
3. Style modifications in `style` tag with CSS parts and CSS properties.
4. Undefined loading product.

<Canvas>
  <Story
    name="Product"
    args={{
      product: productItem,
      variant: "default",
    }}
  >
    {productWithCustomizedProduct.bind({})}
  </Story>
</Canvas>

## Smaller products

Smaller products are usually used in popups and in places where not all information can fit.

<Canvas>
  <Story
    name="Small product"
    args={{
      product: productItem,
      variant: "small",
    }}
  >
    {singleProduct.bind({})}
  </Story>
</Canvas>

## Line products

Line products are typically used in mobile listings.

<Canvas>
  <Story
    name="Line product"
    args={{
      product: productItem,
      variant: "line",
    }}
  >
    {singleProduct.bind({})}
  </Story>
  <Story
    name="Line product 2"
    args={{
      product: productItem2,
      variant: "line",
    }}
  >
    {singleProduct.bind({})}
  </Story>
  <Story
    name="Line product 3"
    args={{
      product: productItem3,
      variant: "line",
    }}
  >
    {singleProduct.bind({})}
  </Story>
</Canvas>

## Loading products

<Canvas>
  <Story name="Product loading" args={{ variant: "default" }}>
    {singleProduct.bind({})}
  </Story>
  <Story
    name="Small product loading"
    args={{
      variant: "small",
    }}
  >
    {singleProduct.bind({})}
  </Story>
  <Story
    name="Line product loading"
    args={{
      variant: "line",
    }}
  >
    {singleProduct.bind({})}
  </Story>
</Canvas>
