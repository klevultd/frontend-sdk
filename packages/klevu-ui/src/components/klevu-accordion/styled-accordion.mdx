import { Controls, Canvas, Meta, Markdown } from "@storybook/addon-docs"
import * as AccordionStories from "./klevu-accordion.stories"
import { KlevuAutoDocs } from "../../../.storybook/blocks/KlevuAutoDocs"
import { KlevuCSSEditor } from "../../../.storybook/blocks/KlevuCSSEditor"

<Meta title="Atoms/Accordion/Examples/Styled Accordion" />

## Styled Accordion

This code example showcases how to create and customize accordion with styling the parts.

<Canvas of={AccordionStories.StyledAccordion} />

Modify example above with following:

<KlevuCSSEditor styleId="styled" />

<KlevuAutoDocs of={AccordionStories} />
