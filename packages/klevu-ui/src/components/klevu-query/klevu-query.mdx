import { html } from "lit-html"
import { ifDefined } from "lit-html/directives/if-defined.js"
import { ArgsTable, Canvas, Meta, Story, Description } from "@storybook/addon-docs"
import { MDXAutoFillMeta } from "../../storybookUtils"
import * as Stories from "./klevu-query.stories"

<Meta title="Non components/Query" component="klevu-query" argTypes={meta.argTypes} parameters={meta.parameters} />

# Klevu-query

<Description>{meta.description}</Description>

<Canvas withSource="open">
  <Story
    name="Query"
    args={{
      type: "search",
      searchTerm: "hoodies",
    }}
  >
    {(args) => html`<klevu-query
      category=${ifDefined(args.category)}
      category-title=${ifDefined(args.categoryTitle)}
      filter-count=${ifDefined(args.filterCount)}
      limit=${ifDefined(args.limit)}
      .manager=${args.manager}
      offset=${ifDefined(args.offset)}
      .options=${args.options}
      recommendation-id=${ifDefined(args.recommendationId)}
      search-term=${ifDefined(args.searchTerm)}
      sort=${ifDefined(args.sort)}
      type=${ifDefined(args.type)}
      update-on-filter-change=${ifDefined(args.updateOnFilterChange)}
    ></klevu-query>`}
  </Story>
</Canvas>

<ArgsTable story="Query" />
