import { ArgsTable, Canvas, Meta, Story, Description } from "@storybook/addon-docs"
import { html } from "lit-html"
import { ifDefined } from "lit-html/directives/if-defined.js"
import { MDXAutoFillMeta } from "../../storybookUtils"
const meta = MDXAutoFillMeta("klevu-chip")

<Meta title="Atoms/Chip" component="klevu-chip" argTypes={meta.argTypes} parameters={meta.parameters} />

# Klevu-chip

<Description>{meta.description}</Description>

## Badge in accent colors

Badge can be in accent colors. Colors are set in css properties and can be changed by overriding them.

<Canvas>
  <Story name="Chip" args={{}}>
    {(args) =>
      html`<klevu-chip selected=${ifDefined(args.selected)} removable=${ifDefined(args.removable)}>A chip</klevu-chip>`
    }
  </Story>
  <Story
    name="Selected chip"
    args={{
      selected: true,
    }}
  >
    {(args) =>
      html`<klevu-chip selected=${ifDefined(args.selected)} removable=${ifDefined(args.removable)}>A chip</klevu-chip>`
    }
  </Story>
  <Story
    name="Removable chip"
    args={{
      removable: true,
    }}
  >
    {(args) =>
      html`<klevu-chip selected=${ifDefined(args.selected)} removable=${ifDefined(args.removable)}>A chip</klevu-chip>`
    }
  </Story>
</Canvas>

<ArgsTable story="Chip" />
